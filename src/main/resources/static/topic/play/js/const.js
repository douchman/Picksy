export const topic = {
    id : null,
    title : null,

    setId(id) {
        this.id = id;
    },

    getId() {
        return this.id;
    },

    setTitle(title) {
        this.title = title
    },

    getTitle(){
        return this.title;
    }
}

export const playRecord = {
    id : null,

    setId(id){
        this.id = id;
    },

    getId() {
        return this.id;
    }
}

export const match = {
    id : null,

    setId(id){
        this.id = id;
    },

    getId(){
        return this.id;
    }
}